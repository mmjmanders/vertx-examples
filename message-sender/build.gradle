plugins {
    id 'java'
    id 'application'
}

mainClassName = 'com.mmjmanders.vertx.sender.MessageSenderVerticle'

dependencies {
    compile "${libs['vertx-web']}"
    compile "${libs['vertx-hazelcast']}"

    testCompile "${libs['vertx-unit']}"
    testCompile "${libs['junit']}"
}
